name: Public ZK build

on:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/public-zk-build.yml"
  repository_dispatch:
    types:
      - public-zk-updated
  workflow_dispatch:
    inputs:
      source_ref:
        description: ZK ref to publish
        required: false
        default: main

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build public ZK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout website
        uses: actions/checkout@v4

      - name: Checkout ZK source
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.client_payload.source_repository || 'christopher-demarco/zettelkasten' }}
          ref: ${{ github.event.client_payload.source_sha || inputs.source_ref || 'main' }}
          token: ${{ secrets.ZK_READ_TOKEN }}
          path: _zk_source

      - name: Configure Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4.6"
          bundler-cache: true

      - name: Install yq
        env:
          YQ_VERSION: v4.44.6
        run: |
          set -euo pipefail
          sudo curl --fail --silent --show-error --location \
            --output /usr/local/bin/yq \
            "https://github.com/mikefarah/yq/releases/download/${YQ_VERSION}/yq_linux_amd64"
          sudo chmod +x /usr/local/bin/yq
          yq --version

      - name: Generate public ZK files
        run: |
          set -euo pipefail
          rm -rf zk
          mkdir -p zk

          find _zk_source -name '*.md' -print0 |
            while IFS= read -r -d '' note; do
              is_public="$(
                yq --front-matter=extract 'tag == "!!map" and .public == true' "$note" 2>/dev/null ||
                  echo false
              )"

              [ "$is_public" = "true" ] || continue

              uid="$(yq --front-matter=extract '.UID // ""' "$note")"
              if [ -z "$uid" ]; then
                echo "missing UID: $note" >&2
                exit 1
              fi

              name="$(basename "$note")"
              if [ -e "zk/$name" ]; then
                echo "duplicate public note filename: $name" >&2
                exit 1
              fi
              cp "$note" "zk/$name"

              if [ -e "zk/$uid" ]; then
                echo "duplicate public note UID: $uid" >&2
                exit 1
              fi
              mkdir -p "zk/$uid"
              cat > "zk/$uid/index.html" <<EOF
          <!doctype html>
          <html lang="en">
          <head>
            <meta charset="utf-8">
            <meta http-equiv="refresh" content="0; url=../$name">
            <link rel="canonical" href="../$name">
            <title>Redirecting...</title>
          </head>
          <body>
            <p>Redirecting to <a href="../$name">$name</a>.</p>
            <script>window.location.replace("../$name");</script>
          </body>
          </html>
          EOF
            done

      - name: Build Jekyll site
        run: bundle exec jekyll build

      - name: Commit generated public ZK
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add zk
          if git diff --cached --quiet; then
            echo "No generated public ZK changes"
            exit 0
          fi
          git commit -m "Regenerate public ZK"
          git push
